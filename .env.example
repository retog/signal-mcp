# Signal MCP Server Configuration

# Required: Your Signal phone number (with country code)
SIGNAL_ACCOUNT="+1234567890"

# Optional: Path to signal-cli binary
# SIGNAL_CLI_PATH="/opt/signal-cli/bin/signal-cli"

# Optional: Timeout for signal-cli commands in milliseconds (default: 30000)
# SIGNAL_TIMEOUT="30000"
